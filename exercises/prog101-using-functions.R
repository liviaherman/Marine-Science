# Fill in the blanks so the answers are all 2. Use help for unfamiliar functions
# if necessary.
log10(100)
log2(4)
ceiling(1.1)

# Find the functions for cosine and sine. What are the cosine and sine of 0?
# The function for cosine and sine are cos() and sin()
cos(0)
sin(0)
#The cos(0) is 1 while the sin(0) is 0

# What do you think the following functions do? Just go off the name, don't look
# them up.
read.csv()
# I think this function has something to do with analyzing a csv file which
# stores data in a spreadsheet like format?
boxplot()
# I believe this function generates a box and whisper plot?
startsWith()
# I think this function tells the computer which data points to start with when
# dealing with a large set of data??
